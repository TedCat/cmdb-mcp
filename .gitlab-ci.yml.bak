stages:
  - deploy

workflow:
  rules:
    - if: $CI_COMMIT_TAG =~ /^v[0-9]+\.[0-9]+\.[0-9]+$/

deploy_job:
  stage: deploy
  tags:
    - ops
  script:
    - docker build . -t harbor.example.com/cmdb-mcp:$CI_COMMIT_TAG
    - docker push harbor.example.com/cmdb-mcp:$CI_COMMIT_TAG